# 交接记录 — 2026-09-17 关机（Round 2）

本轮实验的完整结论见 `revision/ROUND2_RESULTS.md`（本地 + 服务器同路径）。本文件只记录资产处置与恢复步骤。

## 服务器

`ssh -p 40466 root@connect.bjb1.seetacloud.com`（AutoDL，25 核 / 128 GB / RTX 5090，**本次已关机**）

项目路径 `/root/quality_noise`。容器 ID `autodl-container-552540b067-b7a83431`，`/root/autodl-tmp` 与项目盘跨关机保留。

**本轮新增的登录方式**：容器已装入本机公钥（`~/.ssh/id_ed25519`），恢复后可直接用免密 wrapper，不必再走密码：

```bash
~/.dsh_ssh/rsh4 '<command>'      # 远端 shell（key auth，无长度限制）
~/.dsh_ssh/rput4 <local> <remote>  # 上传
~/.dsh_ssh/rget4 <remote> <local>  # 下载
```

旧的 `rsh2`/`rput2`/`rget2`（base64-over-ssh + 密码）仍然可用，但**大文件会超时**——本轮一次 6 KB 的脚本上传卡了 180 秒。新的 `rsh4` 系列是首选。`~/.dsh_ssh/rrun.py`（pexpect 版）因本机无 pexpect 而未启用，可忽略。

## 关机前的资产处置

| 资产 | 位置 | 备注 |
|---|---|---|
| Round-2 全部结果 CSV（13 个） | **本地** `results/revision/round2/` + 服务器同路径 + 持久化盘 | 三份 |
| 实验日志 6 个 | 本地 `results/revision/round2/logs/` | |
| `ROUND2_RESULTS.md` | 本地 + 服务器 | |
| 驱动脚本 10 个 | 本地 + GitHub `4gcy6tzy6n-coder/lowdata` @ `a329129` | 见下 |
| `prepared` 缓存（C100-S20 10 seeds / A40 5 seeds / C100N 5 seeds） | 本地仅 S20；A40 与 C100N **仅服务器** | 见「已知缺口」 |
| DINO 嵌入 1.4 GB | 本地 + 服务器 | |
| C100-S20 训练轨迹 3.7 GB | 服务器持久化盘 `/root/autodl-tmp/lowdata_backup/c100s20_traces.tar.gz` | 跨关机保留 |

**已知缺口**：本轮新增的 A40 与 CIFAR-100N 的 `prepared` 缓存只存在于服务器。本地若要复现 E4/E9，必须先取回
`results/revision/prepared/cifar100/asymmetric0.4/`（5 × 22 数组）与
`results/revision/prepared/cifar100n/cifar100n_human0.4/`（5 × 22 数组）。

## 本轮实验一览

| job | 内容 | 行数 | 在哪跑 |
|---|---|---|---|
| E1 | Combined-noNeighbor，10 seeds × 8 detector × 8 proxy，B=1000 | 640 | 服务器 |
| E2/E3 | 顺序随机化（25 reps）+ clean 池可复用 | 300 | 服务器 |
| E4 | C100-A40 全审计，5 seeds × 8 × 8，B=1000 | 320 | 服务器 |
| E5 | 48 family BH-FDR（sign-flip / Wilcoxon / paired-t） | 48 | 本地 |
| E6 | 高斯模拟，4 γ × 51 λ × 100 reps（扩展网格到 λ=5） | 20,400 | 本地 |
| E7 | detector 强度分箱反转率 | 12 | 本地 |
| E8 | kNN 互信息 I(Q;Z) + 1000 次置换检验 | 80 + 40 | 本地 + 服务器 |
| E9 | CIFAR-100N 人类噪声审计，5 seeds × 8 × 8，B=1000 | 320 | 服务器 |
| E10 | leave-one-detector-out 反转预测 | 168 | 本地 |
| E11 | GLM 方差分解：family vs (proxy, detector)，2000 次置换 | 5 + 2000 | 本地 |
| 粒度审计 | detector 分数分辨率与配对并列率 | 240 | 服务器 |
| 覆盖率回填 | 修复 `noisy_coverage` 恒为 1.0 的列 | — | 本地 + 服务器 |

## 三条主要结论

1. **衰减是真的，反转是条件性的。** `combined_noN`（去掉邻居项、与 proxy 无共享构造）在 knn_agreement 上仍衰减 +0.1787 且 10/10 interval 反转；四个完全解耦的连续 detector 也衰减 +0.095~0.204。决定反转的不是耦合，而是不等式 `Δ > A_global − 0.5`（E10 留一预测 94% 一致）。
2. **人类噪声下衰减最大、反转为零。** CIFAR-100N：label-dependent Δ = +0.1265（knn_agreement +0.1501），对照 floor 仅 +0.0054，但 0/80 反转——因为 detector 太强（A_global 0.61~0.77）。这是最强的新证据。
3. **两个 detector 不可审计。** `neighbor` 只有 11~18 个唯一值、`forgetting` 110~129 个，配对 AUROC 因并列而退化（`neighbor × knn_agreement` 在三套数据上**恰好 0.5000**）。区间门限已正确拦下它们，但论文必须声明审计只对六个连续 detector 有效。

## 本地资产总览

```
results/revision/
├── round2/              本轮 13 个 CSV + logs/ 6 个日志
├── p0_batch/            j1..j5，3,815 行 + P0_BATCH_RESULTS.md
├── p1_alternatives/     j6, j7
├── p1_simulation/       高斯模拟
├── prepared/            仅 C100-S20 十 seeds（A40 / C100N 在服务器）
└── embeddings/dino/
revision/ROUND2_RESULTS.md   本轮完整结论
```

Git：驱动脚本已提交并推送到 `main`（`a329129`）。代理不通时用
`git -c http.proxy= -c https.proxy= push`。`results/` 与报告按策略不入库。

## 恢复服务器后

```bash
# 1) 训练轨迹（仅在需要新 detector 时）
cd /root/autodl-tmp/lowdata_backup && tar xzf c100s20_traces.tar.gz -C /root/quality_noise/

# 2) 免密登录已配好，直接用：
~/.dsh_ssh/rsh4 'cd /root/quality_noise && ls results/revision/round2/'

# 3) 若公钥丢失，重装：
cat ~/.ssh/id_ed25519.pub   # 追加到 /root/.ssh/authorized_keys
```

## Canonical freeze（2026-09-17 第二次关机）

上一次归档 `round2_results.tar.gz` 是**统计修正之前**的快照，已标记
`round2_results.tar.gz.SUPERSEDED`。当前唯一权威归档：

```
/root/autodl-tmp/lowdata_backup/canonical-round2-v1.tar.gz
  956,061 bytes
  sha256 1d0e80603c9053425d53e5cf0e70b6eef9be34d614a535d474b2eaad3ec41acc
  git 1acaa3c（量化内容）/ 7a6d2fa（文档）    schema canonical-round2-v1
```

内容：`ARCHIVE_PROVENANCE.md`（版本/提交/修正清单/头条统计）、`README.md`、
`MANIFEST.sha256.txt`、`results/revision/round2/`（统一后的审计 CSV 与全部派生表）、
`results/revision/p0_batch/j1_bootstrap_all_proxies.csv`、十个驱动脚本。

解包后自检：

```bash
tar xzf canonical-round2-v1.tar.gz && cd canonical-round2-v1
python revision/verify_canonical.py --root .   # 独立实现，不 import 生产者
```

本次 canonical 阶段相对旧快照修掉的问题（详见 ARCHIVE_PROVENANCE.md）：
E5 三个检验方向、48-family 去重与 BH 在报告集合内校正、effect floor 改为带符号
`Δ ≥ 0.01`、primary/coarse/excluded detector 分层、coverage schema 统一、
以及 `load_analysis_set` 误删 `combined_noN` 行。

正文统计口径（core-5 预定义 40 families）：衰减 BH 显著 **28/40**，加符号效应门限
**18/40**，反转 **3/40**。48-family（含 Combined-noN）为敏感性 34/48 → 21/48 → 5/48。
15-family 载信息 proxy 的 15/15 属事后筛选，只入补充材料。

**注意**：本次重启后容器 CPU 配额从 25 核降为 0.5 核（`cpu.max 50000/100000`），
canonical 的后处理改在本地 10 核完成；服务器上只跑了 C100N common-support 回填
（2 秒）与归档打包，未运行任何新实验。下次若需在此容器重跑重计算任务，先确认配额。
