arch: resnet18
pretrained: false
optimizer:
  name: sgd
  lr: 0.1
  momentum: 0.9
  weight_decay: 5.0e-4
scheduler:
  name: cosine
  t_max: 200
epochs: 200
batch_size: 128
num_workers: 4
seed: 0
device: auto        # auto | cuda | mps | cpu
